import { theme } from '@jandies/style';

export const DEFAULT_ICON_SIZE: number = 16;
export const DEFAULT_ICON_FILL: string = theme.colors.black;
export const DEFAULT_ICON_STROKE: string = theme.colors.black;
